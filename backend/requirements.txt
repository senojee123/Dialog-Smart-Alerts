fastapi==0.111.0
uvicorn==0.30.1
python-multipart==0.0.9

# Persistence (B1) — SQLAlchemy async. Postgres in deployment, SQLite for local/CI.
SQLAlchemy[asyncio]>=2.0
alembic>=1.13
asyncpg>=0.29        # production driver (PostgreSQL)
aiosqlite>=0.20      # local/dev/CI driver (SQLite)
paho-mqtt>=2.0.0

